<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/mvc"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:context="http://www.springframework.org/schema/context"
             xmlns:tx="http://www.springframework.org/schema/tx"
             xsi:schemaLocation="http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd http://www.springframework.org/schema/cache http://www.springframework.org/schema/cache/spring-cache.xsd http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd">

    <!-- DispatcherServlet Context: defines this servlet's request-processing infrastructure -->

    <!-- Enables the Spring MVC @Controller programming model -->
    <annotation-driven/>

    <!-- Handles HTTP GET requests for /resources/** by efficiently serving up static resources in the ${webappRoot}/resources directory -->
    <resources mapping="/resources/**" location="/resources/"/>

    <!-- Resolves views selected for rendering by @Controllers to .jsp resources in the /WEB-INF/views directory -->
    <beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <beans:property name="prefix" value="/WEB-INF/views/"/>
        <beans:property name="suffix" value=".jsp"/>
    </beans:bean>

    <context:component-scan base-package="com.lec.sts16_interceptor"/>

    <!-- DataSource 생성 -->
    <beans:bean name="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <beans:property name="driverClassName" value="oracle.jdbc.driver.OracleDriver"/>
        <beans:property name="url" value="jdbc:oracle:thin:@localhost:1521:xe"/>
        <beans:property name="username" value="scott_b"/>
        <beans:property name="password" value="tiger_b"/>
    </beans:bean>

    <!-- Transaction 처리를 위한 PlatformTrasactionManager 객체 생성 -->
    <beans:bean name="platformTransactionManager"
                class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <beans:property name="dataSource" ref="dataSource"/>
    </beans:bean>

    <!-- @Transactional 사용을 위한 설정 -->
    <tx:annotation-driven transaction-manager="platformTransactionManager"/>

    <!-- 마이바티스 설정	-->
    <!-- 마이바티스 한글 : http://www.mybatis.org/mybatis-3/ko/ -->
    <!-- 마이바티스 스프링 설정 : http://www.mybatis.org/spring/ko/sample.html -->
    <!-- 마이바티스 매퍼XML 설정 : http://www.mybatis.org/mybatis-3/ko/sqlmap-xml.html -->
    <!-- 마이바티스 매퍼설정: http://www.mybatis.org/mybatis-3/ko/configuration.html -->

    <!-- 핵심 객체인 SqlSessionFactoryBean 생성 -->
    <beans:bean name="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
        <beans:property name="dataSource" ref="dataSource"/>
        <beans:property name="mapperLocations">
            <beans:list>
                <beans:value>classpath:com/lec/sts16_interceptor/**/*.xml</beans:value>
            </beans:list>
        </beans:property>
    </beans:bean>

    <beans:bean name="sqlSessionTemplate" class="org.mybatis.spring.SqlSessionTemplate">
        <beans:constructor-arg index="0" ref="sqlSessionFactory"/>
    </beans:bean>

    <!-- Interceptor 빈(Bean) 생성 -->
    <beans:bean id="loginInterceptor" class="com.lec.sts16_interceptor.interceptor.LoginInterceptor"/>

    <!-- Interceptor 등록 / 설정 -->
    <interceptors>
    	<interceptor>    
            <mapping path="/**"/>
            <exclude-mapping path="/"/>  <!-- 인터셉터 예외 -->
            <exclude-mapping path="/user/**"/>
            <exclude-mapping path="/board/list"/>  <!-- 인터셉터 예외 -->
            <beans:ref bean="loginInterceptor"/>
    	</interceptor>
    </interceptors>

</beans:beans>
