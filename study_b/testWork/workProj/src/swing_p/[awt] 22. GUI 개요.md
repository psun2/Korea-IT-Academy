AWT, GUI프로그래밍 개요

GUL는 과거에 사용하였던 DOS와 같은 방식의 텍스트 기반 운영체제가
아닌 그래픽을 이용하여 사용자와 프로그램간의 상호작용을 할수 있게 해주는 인터페이스
를 말한다. 자바에서 이러한 그래픽 프로그래밍을 지원하기 위해 나온 것이 바로
AWT인 것이다.


AWT의 기본 개념

AWT는 GUI 프로그래밍을 제작하기 위해 자바에서 제공하는 라이브러리를 모아놓은 것이다.
AWT는 모든 GUI 프로그램에 사용되는 컴포넌트와 툴킷을 제공하고 있으며 JFC와 같은
스윙 및 Java2D의 모태가 되는 개념이다.

자바의 큰 특징이 "한번 작성된 프로그램은 어떤 플랫품에서도 사용 할수 있다."라는
것처럼 AWT는 운영체제에 구애밪지 않고 쓸수 있도록 공통적이고 기본적인 컴포넌트들을
추상화시켜 제공한다. 하지만 운영체제의 리소스를 사용하기 떄문에 AWT를 사용한 자바
GUI프로그램은 실행되는 운영체제에 따라 다르게 보이거나 동작 방식에 차이가 있
수 있다. 그래서 이러한 단점을 극복하기 위해 차후에 나온 것이 JFC 이고 현재는
AWT보다는 스윙을 일반적으로 사용하고 있다.



GUI : Graphical User Interface
 



 

AWT : Abstract Windowing Toolkit
 



- 자바가 처음 나왔을 때 함께 배포된 GUI 라이브러리



- Java.awt 패키지



- native(운영체제)와 응용프로그램 사이의 연결 라이브러리



 

Swing
 



- AWT 기술을 기반으로 작성된 자바 라이브러리



- 모든 AWT 기능 + 추가된 고급 컴포넌트



- 순수 자바 언어로 구현됨, JDK 1.1 ~



- Swing 컴포넌트는 native(운영체제)에 의존하지 않음



- Class 이름이 모두 J로 시작한다. Ex) JButton, JLabel, ...



---------------------------------------------------------------------------------------------------------

AWT Container Component(AWT 컨테이너 컴포넌트)
 



- 다른 컴포넌트를 포함할 수 있는 특별한 GUI 컴포넌트!!!



- 컨테이너가 되기 위해선 반드시 java.awt.Container Class를 상속받아야 한다.



Example) Frame, Panel, Applet, Dialog, Window





 

Swing Component(스윙 컴포넌트)
 



Example) JFrame, JPanel, JApplet, JDialog, JWindow, ...



 

Top Level Container(최상위 컨테이너)
 



- 다른 컨테이너에 속하지 않고 독립적으로 존재하여 출력될 수 있는 컨테이너



Example) JFrame, JDialog, JApplet, ...



- 그러나 이들을 제외한 JPanel, JScrollPane 등 모든 컨테이너는 독립적으로 존재할 수 없고 다른 컨테이너에 부착될 때만 



  화면에 출력된다.



 

Component(컴포넌트)
 



- 컨테이너에 포함되어야 화면에 출력될 수 있는 GUI 객체, 즉 누군가의 도움이 필요한 아이들이다



- 컨테이너 + 컴포넌트를 합친 모든 스윙 컴포넌트들은 javax.swing.JComponent를 상속받는다.



- AWT 컴포넌트들은 모두 ajva.awt.Component를 상속받는다.

맨 바깥에 최상위 컨테이너인 JFrame 컨테이너가 있고, 그 안에 JPanel 컨테이너가 하나 부착되어 있다.



그리고 이 컨테이너에는 JPanel로 만들어진 2개의 컨테이너와 1개의 버튼 컴포넌트가 부착되어 있다.



두 컨테이너 안에는 여러개의 컴포넌트들이 부착되어 있다.



 

Swing Frame(스윙 프레임)
 

- 모든 스윙 컴포넌트를 담는 최상위 GUI 컨테이너


출처 : https://cafe.naver.com/jx007s/5723

- 스윙 프레임 기본 구성



# 프레임 - 스윙 프로그램들의 기본 틀

# 메뉴바 - 메뉻ㄹ이 부착되는 공간

# 컨텐트 팬 - GUI 컴포넌트들이 부착되는 공간

컨테이너


컨테이너와 컴포넌트


컨테이너는 자신의 영역에 컴포넌트를 포함시키고 관리하는 역할을 하며 컨테이너가 다른
컨테이너를 포함할 수도 있다. 컨테이너는 주로 다른 컴포넌트를 포함시켜 사용하는 경우
에 의미가 있다. 달리 표현하면 컨테이너에 컴포넌트를 부착시키지 않으면 독자적으로 화면에
출력할 수가 없고 반드시 컨테이너에 컴포넌트를 부착시켜야만 화면에 출력할 수 있다.
컨테이너의 종류에는 Frame, Window, Panel, Applet, Dialog, FileDialog, ScrollPane이 있고
이러한 컨테이너에 컴포넌트를 부착시키기 위해 add() 메서드 를 사용한다.

Frame 클래스

Frame 클래스는 Window 클래스의 하위 클래스로 일반적인 응용프로그램에서 윈도우를
생성하기 위해 사용되는 클래스다. Frame 클래스의 상위 클래스인 Window 클래스는
타이틀,메뉴 등이 지원되지 않기 때문에 일반적으로 사용하지 않고 Frame 클래스를
사용한다. Frame클래스는 기본적으로 경계선, 타이틀, 메뉴, 시스템 상자(최소한, 최대화, 종료 버튼)
등의 기능을 제공한다. Frame은 다른 윈도우에 속해 있지않은 윈도우로 최상위 레벨 윈도우라한다.

Frame 클래스는 기본 사이즈가 정해져 있지 않기 때문에 setSize(), setBounds() 메서드 등을 이용해서
윈도우의 크기를 설정한 후 setVisible(), show()메서드를 통해서 화면에 출력시킬 수 있다

